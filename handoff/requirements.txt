# Install torch/torchvision FIRST, matched to the server's CUDA version --
# see https://pytorch.org/get-started/locally/ -- then `pip install -r` this.
# Pinning torch here would fight the CUDA build the server needs.
numpy>=1.24
pandas>=2.0
pyarrow>=12.0          # parquet
scipy>=1.10            # spearmanr, wilcoxon
scikit-learn>=1.3      # balanced_accuracy_score, roc_auc_score
pillow>=10.0           # must have WebP support; standard in wheels
opencv-python>=4.8     # ProtoPNet/push.py
matplotlib>=3.7        # ProtoPNet/push.py
huggingface_hub>=0.20  # crop download
